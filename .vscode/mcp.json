{
	"servers": {
		"dynatrace-mcp-server": {
            "url": "https://${env:MCP_SERVER}-mcp-server.whydevslovedynatrace.com",
            "type": "http",
			"headers": {
        	"Authorization": "Basic ${env:MCP_AUTH_TOKEN}"
      		}
		}
	}
}